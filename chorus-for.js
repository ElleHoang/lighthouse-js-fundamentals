const chorus = "Let's dance!";
for (let repeat = 0; repeat < 10; repeat++) { // (counter variable; condition that should be true for loop to continue; incrementing of counter variable)
  console.log(chorus);
}
console.log("Until the sun comes up!");